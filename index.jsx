import { useNetworkStatus } from './web/useNetworkStatus';
import { useDebounce } from './web/useDebounce';
import { useFetch } from './api/useFetch';
import { useGeneratePassword } from './security/useGeneratePassword';

export default {
    useNetworkStatus,
    useDebounce,
    useFetch,
    useGeneratePassword
};